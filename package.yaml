name: repl-server
version: "0.1"

dependencies:
  - base
  - process
  - getopt-generics
  - unix

ghc-options:
  - -threaded

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - hspec
      - silently

executables:
  repl-server:
    main: repl-server.hs
    source-dirs:
      - src
